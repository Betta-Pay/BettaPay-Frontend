import { Toggle } from "@/components/ui";
import type { OnboardingData } from "@/app/onboarding/page";
type Props = { data: OnboardingData; errors: Record<string, string>; onChange: (data: Partial<OnboardingData>) => void };
const anchors = ["Cowry", "ClickPesa", "KachinTech"];
export function StepSettlement({ data, errors, onChange }: Props) { return <section className="space-y-5"><div><h2 className="text-lg font-semibold">Settlement preferences</h2><p className="text-sm text-muted-foreground">Select an anchor to help process your local settlements.</p></div><div className="space-y-3">{anchors.map((anchor) => <button key={anchor} type="button" onClick={() => onChange({ preferredAnchor: anchor })} className={`w-full rounded-lg border p-4 text-left ${data.preferredAnchor === anchor ? "border-primary bg-primary/5" : "border-border"}`}><p className="font-medium">{anchor}</p><p className="text-sm text-muted-foreground">Preferred settlement partner</p></button>)}</div>{errors.preferredAnchor && <p className="text-sm text-destructive">{errors.preferredAnchor}</p>}<div className="flex items-center justify-between rounded-lg border p-4"><div><p className="font-medium">Auto-settle</p><p className="text-sm text-muted-foreground">Settle available balances automatically.</p></div><Toggle checked={data.autoSettle} label="Auto-settle" onClick={() => onChange({ autoSettle: !data.autoSettle })} /></div></section>; }
