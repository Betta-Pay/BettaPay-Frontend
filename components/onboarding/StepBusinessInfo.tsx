import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import type { OnboardingData } from "@/app/onboarding/page";
type Props = { data: OnboardingData; errors: Record<string, string>; onChange: (data: Partial<OnboardingData>) => void };
export function StepBusinessInfo({ data, errors, onChange }: Props) { return <section className="space-y-5"><div><h2 className="text-lg font-semibold">Business information</h2><p className="text-sm text-muted-foreground">This appears on payment records and receipts.</p></div><div className="space-y-2"><Label htmlFor="businessName">Business name</Label><Input id="businessName" autoFocus value={data.businessName} onChange={(event) => onChange({ businessName: event.target.value })} aria-invalid={!!errors.businessName} />{errors.businessName && <p className="text-sm text-destructive">{errors.businessName}</p>}</div><fieldset className="space-y-2"><legend className="text-sm font-medium">Business type</legend><div className="grid grid-cols-2 gap-3">{(["individual", "business"] as const).map((type) => <button key={type} type="button" onClick={() => onChange({ businessType: type })} className={`rounded-lg border p-3 text-left capitalize ${data.businessType === type ? "border-primary bg-primary/5" : "border-border"}`}>{type}</button>)}</div></fieldset><div className="space-y-2"><Label htmlFor="country">Country</Label><select id="country" value={data.country} onChange={(event) => onChange({ country: event.target.value })} className="flex h-10 w-full rounded-md border border-input bg-background px-3 text-sm"><option>Nigeria</option><option>Ghana</option><option>Kenya</option><option>South Africa</option><option>United States</option></select>{errors.country && <p className="text-sm text-destructive">{errors.country}</p>}</div></section>; }
